# bid-toolkit v3.6.0 更新日志

## v3.6.1 (2026-07-29)
- 修复: `按...处理` 字符串匹配改为 `按废标处理/按无效处理/按否决处理` 精确匹配（师父审出）
- 修复: `须提供` 补充进 warn 判断条件
- 优化: ▲强调标识上下文含"参考/详见/例如"时降级为 warn
- 优化: `--bid` 改名为 `--bid-file`，避免命令行混淆
- 新增: `-o report.json` 输出 JSON 格式（支持 CI 自动化集成）
- 新增: 报告底部加原文速查表（行号+完整原文，方便定位）

## v3.6.0 (2026-07-29)

### 🚀 新增功能

#### `bid review` — 招标文件风险扫描（三层审标管线）

对标 tender-review-kit，新增审标能力：

- **Layer 1: 判词库定检** — 284条风险关键词，8分类覆盖：废标判决词、评分格式、合同约束、商务门槛、证明文件、时间节点、强调标识、行业特有风险
- **Layer 2: 上下文判断** — 规则引擎自动区分「致命/警告/信息」三级。可选接入LLM API做更精准判断（`--llm`）
- **Layer 3: 反向覆盖检查** — 审完招标文件后，自动检查投标书是否回应了每个风险项

用法：
```
bid review 招标文件.pdf                    # 出审标报告
bid review 招标文件.pdf -o report.md       # 导出5张清单
bid review 招标文件.pdf --bid 投标书.md     # 审完查覆盖
```

#### bid-form 改进（标书智能生成器）

- 标题脱敏，去掉internal_team标识
- 🕶️ 脱敏导出模式（勾选后替换所有公司名/联系人/电话为______）
- localStorage 草稿自动保存（30秒存一次）
- 项目类型三模板分支（人力外包/货物采购/IT服务自动切换章节标题）
- 投标有效期可配置（默认90天）
- 降级提示（建议安装 bid-toolkit）

### 🔧 优化

- 排除模式：自动过滤目录行、章节标题行、页码行等噪音
- 命中合并：同段落连续风险合并为一个条目，减少冗余
- Layer 2 规则增强：精确识别评分标准引用、必要条件、合同约束
- 报告格式优化：按分类分组展示Top-N，统计摘要完整

### 📦 安装

```bash
pip install bid-toolkit>=3.6.0
```
